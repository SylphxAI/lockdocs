# tiny-schema

Schemas for tiny apps.

## Objects

Build an object schema with `s.object({...})`.

### Strict mode

By default unknown keys are stripped. Call `.strict()` to reject unknown keys instead.

```ts
s.object({ name: s.string() }).strict();
```
