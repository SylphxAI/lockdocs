[package]
name = "lockdocs"
description = "Exact-version library docs for AI agents, straight from your lockfile, offline. MCP server + CLI."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true

[[bin]]
name = "lockdocs"
path = "src/main.rs"

[dependencies]
anyhow = "1"
lockdocs-core = { path = "../lockdocs-core" }
serde_json = "1"
toml_edit = "0.25"
dirs = "6"
