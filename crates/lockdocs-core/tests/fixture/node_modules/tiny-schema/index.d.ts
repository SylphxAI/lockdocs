export declare namespace s {
  /** Creates an object schema from a shape. */
  function object<T>(shape: T): ObjectSchema<T>;
  /** Creates a string schema. */
  function string(): Schema<string>;
}
export interface Schema<T> {
  /** Parses input and throws on failure. */
  parse(input: unknown): T;
}
export declare class ObjectSchema<T> implements Schema<T> {
  /** Rejects unknown keys. */
  strict(): ObjectSchema<T>;
  parse(input: unknown): T;
}
